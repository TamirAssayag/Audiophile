export default function ({ store, redirect }) {
  // console.log(store.getters['cart/cart'].length)
  //   if (!store.getters['cart/cart'].length) {
  //     return redirect({ name: 'index' })
  //   }
}
